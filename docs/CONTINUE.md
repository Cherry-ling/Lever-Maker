# 接续指南（Session Handoff）

> 本文件是会话/对话切换的接续入口。切换后第一条消息让自己的话（或下面模板）读这个文件即可。

> 进度快照（2025-06）：**元素/地形对齐 + 参数区间核对全部收口**；制造机多格尺寸判据已拍板 = **判据 C**（包围盒不超 3×3 且总格数<9，见 level-rules.md §2 制造机）。下一步：编关 agent 设计。

## 项目一句话
Level-Maker：给**已有 Unity 消除游戏**（`twoblocks-frontend/BlockColorMatch`）做 **AI 编关工具（编关 agent）**——AI 理解全部关卡内容并生成合法、可解、有难度的关卡。

- 工作目录：`/Users/lingkunwang/Projects/Level-Maker`（可写）
- 游戏仓库（只读事实源）：`/Users/lingkunwang/Projects/twoblocks-frontend`（Unity 项目在 `BlockColorMatch/`）
- **规则工作文档（唯一事实源）**：`docs/level-rules.md`——新会话第一步先读它
- **核对审计**：`docs/level-rules-vs-repo.md`（仓库证据 + 逐项差异 + 拍板进展）

## 最近进展（2025 参数区间核对阶段）
- **元素/地形对齐已基本收口**：基础规则 + 全部特殊元素已定稿（详见 level-rules.md §1/§2/§2.5/§3）
- **本轮新增核对（参数区间，2025-06）**：
  - 452 关语料 + Level 表统计完成（level-rules.md §6 / 审计 §5）：棋盘 50–75 格（宽≤9 高≤13）、每组 2–6 色块（中位 4）、每关 5–8 色、空位下限 ≥4（占用率 76–92%）、限时主线 90–120s / 高关 150–220s
  - 新发现：**普通 block 也支持 customWidth/customHeight 自定义矩形**（LevelConfig.cs:181/184，语料含 3×3/4×6）—— 与 12 标准形状并列的第三类形状源
  - 制造机尺寸判据证据已收集：82 台/597 产出项中 17 个 custom 3×3、B_2X3/B_CROSS 等超大形状在用；包围盒≤2×2 会拒 48% 现状，总格数<9 只拒 17 项
- **本轮新增拍板（2025-06）**：
  - 门（Door）= **用户暂不管**，不定稿（源码有同色进门消耗但 JSON 无落盘、health 未消费）
  - 双色剥壳：**剥壳后剩下的就是普通颜色方块**（内层 B = 普通单格棋子，不再带双色属性，按普通规则消除）
  - matchBlockOrder = **用户暂不管**（现代码仅上报不拦截，如用按"纯数据采集"设计）
  - hasCat 语义 / 限时 baseTime 换算 / hash 用途 = **用户暂不管**
  - 绑定×箭头叠加 = **编关阶段先不做**（不生成"绑定组内成员带箭头"的配置）
  - 区域叠加 = **装修区域可盖在无色区域上方**（顶层元素，允许叠加）；**无色区域可在下方区域上层叠放（2025 新扩展）**——区内无棋子→解开下方区域→按无色区域规则走（语料无实例，编关扩展规则）
  - 制造机产出 = **支持多格形状，但尺寸必须小于 3×3**（判据 [待确认]：默认理解为包围盒 ≤ 2×2）；**拖出操作计为 1 步**（难度统计 +1）
- **已定稿核心结论速记**：形状=12 种真实 BlockType（四向旋转无镜像）/ 移动=拖拽跟随+当前格位结算 / 墙=仅 CYCLE_PIPE / 装修区域+冰冻方块=按用户对齐规则 / ColorTrain=按源码（端部消耗、不读 tracks）/ 计时=首选中后开始、限时来自关卡上下文 / 可玩色 12 种含黑白

## 已定稿 vs 仍待确认（详见 level-rules.md §5）
- **已定稿**：基础规则 · 12 形状（+自定义矩形块） · 无色方块 · 搓冰机 · 双色方块 · 装修区域 · 冰冻方块 · 冰面地形 · 箭头 · 手动制造机 · 颜色筛选 · 颜色火车 · 无色区域 · 绑定方块 · 定时炸弹 · 下方区域 · 墙 · stepLock · 逻辑层 · **参数区间核对**
- **已拍板"暂不管"**：7 钥匙 / 8 星·猫 / Door / matchBlockOrder / hasCat / baseTime 换算 / hash 用途 / 绑定×箭头叠加
- **仍待确认（推进项）**：障碍墙 100–104 破坏链 · 下方区域下层嵌套行为 · 无色区域禁配元素完整清单 · 关卡数据格式样例（均为 agent 设计阶段的细化项，不阻塞规则定稿）

## 待办（按优先级）
1. **[已完成] 制造机多格尺寸判据**：用户拍板 = 判据 C（包围盒不超 3×3 且总格数<9；语料保留 95.1%，仅拒 B_1X4/B_1X5/3×3 整块）；对比表与证据在 level-rules.md §2、审计 §5.2
2. **[已完成] 参数区间核对**：452 关语料 + Level 表统计完成（棋盘 50–75 格 / 每组 2–6 色块 / 空位 ≥4 / 限时 70–360s，详见 level-rules.md §6）
3. **编关 agent 设计**：数据 schema → 合法性与格式校验器 → 可解性求解器（需建模：箭头轴向可达 / 炸弹倒计时含暂停 / 火车车厢队列 / 颜色门控 / 无色区域 / 绑定体移动 / 逻辑层级）→ 生成策略 + 难度模型（最少步数 + 限时换算）
4. **收集现有关卡语料**（452+ 关卡已就位，AI 编关对齐基准 + 测试集，待用户确认后抽取）

## 新会话开场白模板
> 继续 Level-Maker 项目。先读取 `/Users/lingkunwang/Projects/Level-Maker/docs/CONTINUE.md` 和 `docs/level-rules.md`；核对证据看 `docs/level-rules-vs-repo.md`。当前状态：元素/地形对齐 + 参数区间核对全部收口，制造机多格尺寸判据已拍板（判据 C）；下一步进入编关 agent 设计（数据 schema → 校验器 → 求解器 → 生成策略，见待办）。

## 注意事项
- 规则与待办全部在项目文件里，**不依赖旧会话上下文**
- 游戏仓库只读，只改 `Level-Maker/docs/`
- 规矩：只看关卡编辑方面逻辑；以仓库代码为准（仓库 .md 可能过期）；用户确认过的以用户拍板为准
